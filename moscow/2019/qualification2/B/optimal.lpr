program optimal;

uses
  SysUtils;

var
  x, a: string;
  n, t, i: integer;

begin
  readln(t);
  readln(n);
  Assign(input, 'tests/0' + IntToStr(t));
  Assign(output, 'tests/0' + IntToStr(t) + '.a');
  reset(input);
  rewrite(output);
  for i := 1 to N do
  begin
    Readln(x);


    writeln(a);
  end;
end.
