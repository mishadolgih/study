#include <iostream>
#include <fstream>

using namespace std;

int main() {
    freopen("tests/00", "r", stdin);
    string a, b, c;
    while (cin >> a >> b >> c) {
        cout << a << " " << b << " " << c << "\n";
        string s = a + b + c;
        bool dotfound = (s.find('.') != string::npos);
        cout << dotfound << "\n";
        bool letterfound = (while ())

//                letterfound=true;
//            }
//            dotfound= pos('.', s)>0;
//    }
//    cout <<  ;
    }
    return 0;
}
